<%= form_for(@propuesta, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @propuesta.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@propuesta.errors.count, "error") %> impidió que el/la propuesta se guardara:</h4>

      <ul>
      <% @propuesta.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :numero, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :numero, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :nombre, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :nombre, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :estado, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.check_box :estado, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :proyecto, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :proyecto, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
