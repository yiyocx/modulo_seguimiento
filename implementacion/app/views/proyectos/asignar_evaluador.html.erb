<h1>Asignación de Evaluador para Condonación</h1>

<%= form_for :proyectos, url: asignar_evaluador_proyectos_path, method: :get, :html => { :class => "form-horizontal" } do |f| %>
    
  <div class="form-group">
    <%= f.label "Convocatorias de formación:", :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.select(:conv_seleccionada, Proyecto.where(tipo: 0).map { |p| ["#{p.nombre_convocatoria} #{p.num_convocatoria}", p.id] }, {:prompt => 'Select. Convocatoria'}, {:class => "form-control chosen-select"}) %>
    </div>
  </div>

  <div class="form-group">
      <%= f.label "Becarios de la convocatoria con informe final:", :class => "col-sm-2 control-label" %>
      <div class="col-sm-10" id="div-select">
        <%= f.collection_select :becario_seleccionado, [], :id, :nombre, {}, {:class => "form-control"} %>
        <a href="#" class="btn " data-toggle="modal" data-target="#basicModal">Definir Evaluador</a>
        <div class="modal fade" id="basicModal" tabindex="-10" role="dialog" aria-labelledby="basicModal" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                    <h4 class="modal-title" id="myModalLabel">Definir Evaluador</h4>
                    </div>
                    <div class="moda1l-body">
                        <h3> (Corresponde al caso de uso EVA05 - Agregar y seleccionar evaluador)</h3>
                        <h5>La asignación del evaluador se ha realizado exitosamente!</h5>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default" data-dismiss="modal">Cerrar</button>
                        <button type="button" class="btn btn-primary">Guardar</button>
                </div>
            </div>
          </div>
        </div>
        |<%= link_to "#", :class => 'btn' do %>
          Ver registro
        <% end %>
        |<%= link_to "#", :class => 'btn' do %>
          Descargar Informe Final
        <% end %>

      </div>
  </div>

<% end %>
